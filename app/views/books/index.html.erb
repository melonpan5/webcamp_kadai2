<div class="row">
	<div class="col-xs-3">
 <!-- エラーメッセージ表示-->
<!-- エラーメッセージ表示-->

		<h2>User info</h2>
		<%= render 'users/profile', user: current_user %>
		<!--メモ　右側の指定に注意。そこを間違えるとエラーが発生して表示されない -->

		<h2>New book</h2>
		<%= render 'books/newform', newbook: @newbook %>

	</div>

	<div class="col-xs-9">
		<h2>Books index</h2>
		<!--books一覧 -->
		<table class="table table-hover table-inverse">
			<thead>
				<tr>
					<th></th>
					<th>Title</th>
					<th>Opinion</th>
					<th></th>
					<th></th>
					<th colspan="3"></th>
				</tr>
			</thead>
			　<% @books.each do |book| %>
				<%= render partial: "book_index", locals: { book: book } %>
		<% end %>

		</table>

		<!--books一覧 -->


	</div>
</div>